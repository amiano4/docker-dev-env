<VirtualHost *:80>
  ServerName localhost
  ServerAlias www.localhost
  ServerAdmin webmaster@localhost

  ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000${DOCUMENT_ROOT}/

  DocumentRoot ${DOCUMENT_ROOT}

  ErrorLog ${APACHE_LOG_DIR}/error.log
  CustomLog ${APACHE_LOG_DIR}/access.log combined

  <Directory ${DOCUMENT_ROOT}>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    DirectoryIndex index.php
  </Directory>
</VirtualHost>

<VirtualHost *:80>
  ServerName my-projects.local
  ServerAlias www.my-projects.local
  ServerAdmin webmaster@my-projects.local

  ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000${DOCUMENT_ROOT}/

  DocumentRoot ${DOCUMENT_ROOT}

  ErrorLog ${DOCUMENT_ROOT}/error.log
  CustomLog ${DOCUMENT_ROOT}/access.log combined

  <Directory ${DOCUMENT_ROOT}>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    DirectoryIndex index.php
  </Directory>
</VirtualHost>

